PICKUP

Completed tasks:
server/client SSL and message passing
server/client ready for custom functionality
generated authorization credentials

TODO:
cryptopp interface:
	No variables (purely functional)
	Generate and return key and IV (to be saved independently)
	AESGCM encrypt-and-write to file given key and IV
	AESGCM decrypt-and-read from file given key and IV
	HKDF key generation

Server:
	logfile generation
	logfile structure
	query implementation (checking patientid)
